dnl *************************************************************
dnl
dnl Process this file with autoconf to produce a configure script
dnl 
dnl memory leak tests: valgrind --tool=memcheck --leak-check=yes <progname>
dnl
dnl profiler tests: compile with -pg flag + execute code (-> write gmon.out)
dnl                 run gprof executable gmon.out
dnl
dnl disable assertions defining NDEBUG (see assert.h)
dnl
dnl
dnl execute next commands to create Makefiles and compile (-O2 )
dnl
dnl   export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/home/horizon/temp/ccruncher/share/xercesc-2.6/lib
dnl
dnl   aclocal
dnl   autoconf
dnl   automake -avc --warnings=all --foreign
dnl   ./configure
dnl   make
dnl   make check
dnl   make install
dnl
dnl *************************************************************

dnl AC_PREREQ(2.52)
AC_INIT(creditcruncher, 0.1)

AM_INIT_AUTOMAKE

dnl AC_ARG_ENABLE(debug, [  --enable-debug turn on debugging [default=no]], enable_debug="$enableval", enable_debug=no)
dnl if test x$enable_debug = xyes ; then
dnl   AC_DEFINE(GNOME_ENABLE_DEBUG)
dnl fi

AC_PROG_CXX
AC_PROG_CC
dnl AC_PROG_INSTALL

dnl optimize executable code using -O2 option (important!)
dnl CXXFLAGS="-g -pg"
CXXFLAGS="-O2"
AC_SUBST(CXXFLAGS)

dnl AC_CHECK_FUNCS(clock)

AC_OUTPUT([Makefile src/Makefile])
