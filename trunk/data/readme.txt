
This directory is intended to contains the output files
generated by ccruncher. Use like this:

ccruncher -vf --path=$CCRUNCHER/data/YYYYMMDD input.xml

The flag -f will force to create the YYYYMMDD/ directory
under $CCRUNCHER/data/ (this directory). All output files
will be placed there.
